# MONOBASE SEO Improvement Report

作成日：2026-09-11

## 1. 現状分析

MONOBASEは Python 製ジェネレーター（`build.py`）が `content/articles.json`（記事の唯一の原稿）と `content/site.json`（サイト設定・カテゴリー定義）から全HTMLを生成する静的サイトで、`main` への push を契機に GitHub Actions がビルド・デプロイする構成です（管理画面 `admin.html` からの編集も、最終的にはこのJSONへのコミットになります）。

調査の結果、要求されていた技術SEO要素はすでに高い水準で実装済みでした。

| 項目 | 状態 |
|---|---|
| title / description | 記事ごとに個別生成。`｜`区切りでサブ意図を添えるテンプレートが定着済み |
| H1/H2/H3 | `<h1>`は記事タイトル、`<h2>`は `summary` / `good_for` / `not_for` / `highlights` / `pros・cons` / `sections[].heading` / `faq` などキーごとに自動生成 |
| structured data | `Article`・`BreadcrumbList`・（レビューは）`Product+Review`・`FAQPage` を自動出力。`rating`/`spec`が無い記事は自動でそれらのLDも省く安全設計 |
| sitemap.xml / robots.txt | ビルド時に自動生成。`admin.html`は`robots.txt`でcrawl除外済み |
| canonical / OGP | `head()`関数で全ページ共通生成 |
| 内部リンク | ①`related_map()`＝カテゴリー・サブカテゴリー・タグの近さで「関連記事」を自動選出（被リンク数を均す設計）、②`link_past_articles()`＝本文中に他記事の商品名が出てきたら自動でリンク化（1記事最大5本）、③`next_problem.items[].link_url/link_label`＝手動で自然文アンカーを指定できる導線（**今回まで未使用**）、④`products[]`⇄`featured_in()`＝特集記事↔個別レビューの双方向リンクを自動生成 |
| カテゴリーページ・タグ | `category-{cat}.html` / `category-{cat}-{sub}.html` を自動生成。カテゴリー×サブカテゴリーの2階層 |
| 新着記事・ランキング | トップページに実装済み（ランキングはGA4連携） |
| FAQ | `faq[]`から本文の`<details>`アコーディオンと`FAQPage`のJSON-LDを両方生成 |
| パンくず | `crumb_bar()` / `breadcrumb_ld()` |
| 記事テンプレート | `kind`（review/roundup/guide/sale/howto）でセクション構成が切り替わる設計。既に「選び方ガイド」型（`kind:"guide"`）のハブ記事フォーマットが1本（`humidifier-buying-guide`）存在していた |

**重要な発見**：内部リンクの「自然な文章でのアンカーテキスト」を実現する `next_problem.items[].link_url/link_label` という仕組みは既にテンプレート側に実装されていましたが、既存73記事のどれも使っていませんでした。今回のクラスター構築はこの仕組みを実際に使う最初の事例になります。

## 2. 発見した問題

- **「商品名＋口コミ」だけに依存したtitleは、想定より少なかった**。公開73記事のうち、タイトルに`｜`区切りのサブ意図が無い記事は1本（`スカルプD ボリュームパックコンディショナー 口コミ`）のみ。`write_article.py`の生成テンプレートが既にサブ意図を含める設計になっており、Task 3で懸念されていた問題は大部分すでに解消済みでした。
- **FAQも全記事が3問以上を保持**しており、極端に薄い記事はありませんでした。
- 一方で、**「悩み・比較・選び方」という検索母数の大きい入口記事（商品非依存の情報記事）がほぼ存在しない**ことが最大の欠落でした。`feature/guide`カテゴリーの記事は2本（`humidifier-buying-guide`、`amazon-sale-calendar`）のみで、他の全カテゴリーには「選び方ハブ」が1本もありませんでした。
- **同一商品に対する重複記事**を1件確認：レコルト RSY-2（自動調理ポット）が `recolte-rsy-2` / `recolte-rsy-2-2` / `rsy-2-recolte` の3記事に分かれています。検索意図の切り分けが明確でなく、カニバリゼーションのリスクがあります（Section 9・11参照。今回は既存記事の削除・統合は行っていません）。
- 複数商品の在庫がありながらハブ記事が無いクラスターが多数（Section 3）。

## 3. 優先コンテンツクラスター

既存記事の解析から、優先度の高いクラスターを5つ抽出しました。

### クラスター1：冷蔵庫マット（★今回実装）

| 項目 | 内容 |
|---|---|
| 既存記事 | `sns-s-m-l`（ゼロキーパー 冷蔵庫マット 口コミ） |
| 親記事候補 | 冷蔵庫マットの選び方（必要性・サイズ・素材） |
| 関連記事候補 | いらない/デメリット、サイズの選び方、PVC対ポリカーボネート、賃貸の原状回復 |
| 検索意図 | 悩み（いらない・デメリット）／比較（素材）／選び方（サイズ） |
| 想定読者 | 賃貸で原状回復を気にする人、床暖房のあるキッチンの人 |
| 内部リンク先 | `sns-s-m-l`（既存レビュー） |
| 優先度 | **High** |
| 優先理由 | ユーザー提示のワークサンプルと完全一致。既存記事1本を核に、必要性・サイズ・素材という独立した検索意図へ無理なく展開できた |

### クラスター2：敷布団・マットレス（未実装・次点候補）

| 項目 | 内容 |
|---|---|
| 既存記事 | `90n-s-sd-d-q-k`（低反発マットレストッパー）、`a034`（布団7点セット）、`furniture-20260908`（フェザー布団セット）、`furniture-20260909`（高反発マットレス）、`granfort-justright`（敷布団） |
| 親記事候補 | 敷布団・マットレスの選び方（厚み・硬さ・直敷き可否） |
| 関連記事候補 | 直敷きできる敷布団の条件／低反発と高反発の違い／洗える敷布団の選び方／何cm厚を選ぶべきか／セット品と単品どちらがいいか |
| 検索意図 | 比較（低反発 高反発 違い）／選び方（厚み・サイズ）／悩み（直敷き可否・へたり） |
| 想定読者 | フローリングに直接布団を敷く一人暮らし、ベッド代替で検討する人 |
| 内部リンク先 | 上記5記事すべて |
| 優先度 | **High** |
| 優先理由 | 既存記事数が最多（5本）で、かつ1本もハブが無い。直敷き可否は5記事に共通する重要な検索意図 |

### クラスター3：ペットシーツ（未実装・次点候補）

| 項目 | 内容 |
|---|---|
| 既存記事 | `pet-20260906`（まとめ買いケース）、`pet-20260908`（超薄型）、`pet-20260909`（薄型・大容量） |
| 親記事候補 | ペットシーツの選び方（厚さ・吸収量・サイズ） |
| 関連記事候補 | 薄型と厚型の違い／レギュラーとワイドどちらを選ぶか／まとめ買いの保管方法／におい対策 |
| 検索意図 | 比較（薄型 厚型 違い）／選び方（サイズ・吸収量）／用途別（多頭飼い・留守番） |
| 想定読者 | 多頭飼い・こまめな交換を重視する飼い主 |
| 優先度 | **Medium** |
| 優先理由 | 記事3本と規模は中程度だが、サイズ・厚さの比較意図が明確で書きやすい |

### クラスター4：モバイルバッテリー・急速充電器（未実装・次点候補）

| 項目 | 内容 |
|---|---|
| 既存記事 | `anker-621-maggo-battery-review`（MagGo 薄型）、`anker-prime-power-bank-20100mah-220w-with-anker-pr`（20100mAh 220W） |
| 親記事候補 | モバイルバッテリーの選び方（mAh・出力Wの目安） |
| 関連記事候補 | 5000mAhと10000mAh・20000mAhの違い／何W あれば足りるか／MagSafe対応の注意点／飛行機持ち込みのWh制限 |
| 検索意図 | 比較（mAh 違い）／選び方（何mAh・何W）／用途別（出張・旅行） |
| 想定読者 | 出張・旅行でスマホとノートPCを同時に充電したい人 |
| 優先度 | **Medium** |
| 優先理由 | ユーザー提示の検索意図例（5000mAh 10000mAh 違い）に直結し、既存2記事の容量帯がちょうど対比になる |

### クラスター5：ヘアアイロン（SALONIA）（未実装・カニバリゼーション対策を兼ねる）

| 項目 | 内容 |
|---|---|
| 既存記事 | `salonia`、`salonia-hk-slall`（15/24/35mm）、`salonia-salonia-2way-hk-2way-slall`（2WAY 32mm） |
| 親記事候補 | ストレートアイロンの選び方（太さ・海外対応） |
| 関連記事候補 | 太さ別の向き不向き（15/24/32/35mm）／海外電圧対応の見分け方／2WAY（コテ兼用）は必要か |
| 検索意図 | 選び方（太さ・mm）／悩み（海外対応・2WAYの要否） |
| 想定読者 | 前髪だけ整えたい人〜ロングヘアを巻きたい人まで髪型が幅広い層 |
| 優先度 | **Medium** |
| 優先理由 | 既存3記事が同ブランド・近い型番で内容が近接しており、検索意図が重複しがち。ハブで「太さ別にどれを読むべきか」を明示すればカニバリゼーションの解消と回遊の両方に効く |

## 4. 新規作成した記事

冷蔵庫マットクラスターに4記事を新規作成しました（すべて `published: true`）。

| slug | title | kind |
|---|---|---|
| `fridge-mat-guide` | 冷蔵庫マットの選び方｜必要性・サイズ・素材の比較ポイント | guide（ハブ） |
| `fridge-mat-is-it-necessary` | 冷蔵庫マットはいらない？必要性とデメリットを床への影響から整理 | guide |
| `fridge-mat-size-guide` | 冷蔵庫マットのサイズの選び方｜測る場所とはみ出したときの影響 | guide |
| `fridge-mat-material-comparison` | 冷蔵庫マットの素材比較｜PVCとポリカーボネートの違い | guide |

方針として、ユーザー指示の「同一検索意図は統合する」に従い、「いらない／必要ない／意味ない」「デメリット／後悔／向かない人」「カビ・湿気」「賃貸で必要か」は1本（`fridge-mat-is-it-necessary`）に統合しています。当初5〜10本という目安がありましたが、既存の裏付け（実際にレビューした商品は1点のみ）を超えて数を水増しすると「SEO目的だけの薄い記事」「架空の仕様の断定」につながるため、**確実に書ける4本に絞りました**。他クラスターの展開候補はSection 11に記載しています。

各記事は `docs/review-rules.md` の機械チェック対象パターン（`VAGUE_RIVAL`・`CARE_CLAIM`・`AI_PHRASE`）に抵触せず、本文文字数は `tools/check_articles.py` の `body_chars()` で5,000字以上（公式仕様なしのため下限5,000字ルールを適用）を満たしています。

## 5. 改善した既存記事

- `sns-s-m-l`（ゼロキーパー 冷蔵庫マット）：`tags`に「選び方」を追加し、`next_problem.items`にハブ記事・必要性記事への導線を2件追加。構成（キーの並び）・既存の値は変更していません。
- `d-d-d-angfa-pu5`（スカルプD ボリュームパックコンディショナー）：既存の`description`に既に含まれていた「仕上がりの軽さ」「容量の注意点」という情報をtitleに反映（Section 6参照）。本文・FAQ・descriptionは変更していません。

## 6. 変更したtitle / description

| slug | 変更前 | 変更後 |
|---|---|---|
| `d-d-d-angfa-pu5` | スカルプD ボリュームパックコンディショナー 口コミ | スカルプD ボリュームパックコンディショナー 口コミ｜仕上がりの軽さと容量の注意点 |

descriptionの変更はありません（既存73記事の`description`はすでに複数意図を含む文で書かれており、書き換えの必要はないと判断しました）。

## 7. 追加した検索意図

冷蔵庫マットクラスターで新たにカバーした検索意図（ユーザー提示の分類に対応）：

- **A. 悩み・疑問**：「冷蔵庫マット いらない」「冷蔵庫マット カビ」「冷蔵庫マット 必要ない」
- **B. デメリット・失敗回避**：「冷蔵庫マット デメリット」「冷蔵庫マット 後悔」「冷蔵庫マット 向かない人」
- **C. 比較**：「PVC ポリカーボネート 違い」
- **D. 選び方**：「冷蔵庫マット 選び方」「冷蔵庫マット サイズ」
- **E. 用途別**：「冷蔵庫マット 賃貸」「冷蔵庫マット 床暖房」

既存レビュー記事（`sns-s-m-l`）は「ゼロキーパー 口コミ」に加えて、ハブ経由で上記すべての検索意図から到達できる構造になりました。

## 8. 内部リンク変更

- `fridge-mat-guide`（ハブ）→ 3記事へ`next_problem`の明示リンク（3件）
- `fridge-mat-is-it-necessary` ⇄ `fridge-mat-size-guide` ⇄ `fridge-mat-material-comparison`：相互に`next_problem`リンク
- `fridge-mat-guide`は`products[]`に`sns-s-m-l`を登録 → ビルド時に`featured_in()`が自動で`sns-s-m-l`側に「この商品を比較した特集」の逆リンクを生成（**この仕組み自体はテンプレートに既存だが、実際に使われたのは今回が初**）
- `sns-s-m-l`の`next_problem`にハブ・必要性記事への導線を追加
- アンカーテキストはすべて「冷蔵庫マットの選び方ガイドを見る」のように内容が分かる文言にし、「こちら」は使用していません
- 本文中の商品名（例：「冷蔵庫マット」）は`link_past_articles()`により自動でも1回リンク化されることをスクラッチビルドで確認済み

ビルド確認（別ディレクトリへのコピー上で実施。詳細はSection 10）で、新設記事を含む全272件の内部リンクhrefのリンク切れが0件であることを確認しました。

## 9. カニバリゼーション対策

- 新規4記事は着手前に既存73記事の`title`・`tags`・`category`を全件確認し、同一検索意図の記事が無いことを確認してから執筆しました。
- レコルト RSY-2の3記事重複（`recolte-rsy-2` / `recolte-rsy-2-2` / `rsy-2-recolte`）を発見しましたが、既存記事の削除・統合は本タスクの依頼範囲を超え、内容の当落を精査する追加調査が必要なため、**今回は変更せず、Section 11の要対応事項として記録するにとどめました**。
- SALONIA 3記事（クラスター5）も同様に、統合ではなく「ハブで役割分担を明示する」方向で解消することを推奨します（Section 3）。

## 10. 技術SEO変更

- 技術SEO実装（JSON-LD／sitemap／canonical／OGP／パンくず）に変更は加えていません。既存の自動生成の仕組みが新規記事にもそのまま適用されることを確認しました。
- **ビルド確認の方法**：このセッションの実行環境では、Gitで管理済みの生成ファイル（`index.html`・`articles/*.html`・`sitemap.xml`等、約150ファイル）への上書きがサンドボックス制約でブロックされたため、リポジトリ全体をスクラッチ領域にコピーし、そこで`python3 build.py`を実行して検証しました（本番のリポジトリ内ファイルは一切削除していません）。
  - ビルド結果：`✅ ビルド完了：151 ファイル／公開記事 77 本 / 下書き 6 本`（新規4記事分、73→77に増加）
  - `sitemap.xml`に新規4記事のURLが追加されていることを確認
  - `tools/check_articles.py`を実行し、新規記事が警告なしで検証を通過することを確認（他記事の既存warningは今回の変更と無関係）
  - 内部リンク272件のリンク切れ0件を確認
  - `category-daily-storage.html` / `category-feature-guide.html`に新規記事が正しく一覧表示されることを確認
- CSS/JSは変更していないため、`tools/check_layout.py`（画面崩れ検査）は今回実行対象外です。

## 11. 今後作成すべき記事候補

優先度順に、Section 3のクラスター2〜5を推奨します。

1. **敷布団・マットレスの選び方**（既存5記事の親記事＋「直敷き可否」「低反発/高反発の違い」の2本）
2. **モバイルバッテリーの選び方**（mAh・出力Wの比較。既存2記事がちょうど対比になる）
3. **ペットシーツの選び方**（厚さ・吸収量の比較）
4. **ストレートアイロンの選び方**（太さ別。SALONIA3記事のカニバリゼーション解消も兼ねる）
5. レコルト RSY-2の3記事（`recolte-rsy-2` / `recolte-rsy-2-2` / `rsy-2-recolte`）の内容差分を精査し、統合または役割分担を決める
6. 加湿器クラスターは既にハブがあるため、「加湿器 電気代」「加湿器 デメリット」のような悩み系サブ記事を1〜2本追加すると投資対効果が高い

## 12. 次に優先する改善

1. `seo-rank-watch`スキル（週次でSearch Consoleのデータを見る仕組みが既に稼働中）で、新設した4記事のクエリ表示状況を2〜4週間観察する
2. 敷布団・マットレスクラスター（クラスター2）のハブ記事を次の優先実装として着手する
3. レコルトRSY-2の重複3記事の統合方針を決定する
4. `check_layout.py`は今回未使用のため、次にCSS/JSを触る作業と合わせて実行する
